{
  "name": "auth_microservice",
  "version": "1.0.0",
  "description": "This project is a microservice built on Express for authentication using Json Web Tokens (JWT). Authentication is made with email and password. Users are identified with an uuid. This microservice supports token invalidation through a Redis instance, using the whitelist approach.",
  "main": "index.js",
  "scripts": {
    "prestart": "npx prisma migrate deploy",
    "start": "node dist/index.js",
    "start:dev": "ts-node src/index.ts",
    "start-inspect": "node --inspect dist/index.js",
    "dev": "nodemon src/index.ts",
    "clean": "rm -rf ./dist",
    "clean-build": "npm run clean && npm run build",
    "normal-build-start": "npm run build && npm run start",
    "normal-build-start-inspect": "npm run build && npm run start-inspect",
    "clean-start": "npm run clean && npm run build && npm run start",
    "clean-start-inspect": "npm run clean && npm run build && npm run start-inspect",
    "build": "tsc",
    "lint": "eslint --fix src"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@prisma/client": "^6.8.2",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "ioredis": "^5.6.1",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.27.0",
    "@types/bcrypt": "^5.0.2",
    "@types/cors": "^2.8.18",
    "@types/express": "^5.0.2",
    "@types/jsonwebtoken": "^9.0.9",
    "@types/morgan": "^1.9.9",
    "@types/node": "^22.15.18",
    "eslint": "^9.27.0",
    "globals": "^16.1.0",
    "prisma": "^6.10.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.32.1"
  }
}
